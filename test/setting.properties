# Configurazione applicazione
app.name=MyApplication
app.version=1.0.0
app.author=SimoneRossi

# Impostazioni database
db.host=localhost
db.port=5432
db.name=mydb
db.user=admin

# Modalità debug
debug.enabled=true
debug.level=INFO

# Timeout
connection.timeout=30
request.timeout=60
